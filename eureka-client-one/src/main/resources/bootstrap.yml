spring:   
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-center
      uri: http://localhost:8759/
      profile: dev
      label: master
  rabbitmq:
      host: localhost
      port: 5672
      username: guest
      password: guest